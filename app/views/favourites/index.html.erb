<h1>My Garage 🚗</h1>

<ul>
  <% @favourites.each do |favourite| %>
    <li>
      <%= favourite.car.brand %> | <%= favourite.car.model %>
      <%= link_to 'Delete', favourite_path(favourite), data: { turbo_method: :delete, confirm: "Are you sure?" }, class: "btn btn-danger" %>
    </li>
  <% end %>
</ul>

<hr>
<%= link_to "Back to all cars", cars_path %>
